# Load nix env
use nix

# Don't let opam root escape to ~/.opam
export OPAMROOT=$(pwd)/.opam

# Applies the environment generated by running opam-env.
# Runs "direnv dump" and then wrap the results with direnv_load.
direnv_load opam exec direnv dump
